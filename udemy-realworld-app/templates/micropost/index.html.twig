{% extends 'base.html.twig' %}

{% block title %}
	MICROPOST / INDEX
{% endblock %}

{% block body %}
	{% for id, post in posts %}
		<h3>{{ post.title | upper }}</h3>
		<p>{{ post.time | date('Y-m-d H:i:s') }}</p>
	{% endfor %}
{% endblock %}
