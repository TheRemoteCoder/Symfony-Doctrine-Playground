{% extends 'base.html.twig' %}

{% block title %}
	BLOG / INDEX
{% endblock %}

{% block body %}
	{% for id, post in posts %}

		<p>
			<a href="{{ path('blog_show', {'id':id}) }}">{{ id }}</a>
		</p>
		<h3>{{ post.title }}</h3>
		<p>{{ post.text }}</p>

	{% endfor %}
{% endblock %}
